#include <stdio.h>

int main() {
    if (arr[0] > 0 {
        printf("This will not compile\n");
    }

    // Incorrect: unmatched braces
    for (int i = 0; i < 3; i++) {
        printf("%d\n", arr[i]);
    // missing closing brace for for-loop

    // Incorrect: unmatched double quote
    char *bad_str = "Oops;

    return 0;
}
